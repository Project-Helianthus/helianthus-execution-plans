# Superseded W28 draft

This directory is an inert historical marker. The W28 draft was superseded by
`fronius-modbus-multivendor-v3-w29-26.implementing/`.

The former executable plan, validator, reports, attestations, hashes, and dispatch
machinery were removed from the working tree during process simplification. They remain
available through Git history when historical analysis is required, but they are not an
execution entrypoint and must not be used to determine current readiness.
