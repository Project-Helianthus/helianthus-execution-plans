# Executable issue map

Each row is one future executable issue owned by exactly one repository. The existing
`.github` governance issue creates the two public Modbus repositories; their destination
bootstrap rows depend on it and run only after each repository exists. Private repository
governance creation remains deferred to M0-04, followed by destination bootstraps M0-05
and M0-07, all under future explicit authorization. This map contains 46 issues.
Milestones are groupings only.

FMV3-M3-03 rejects same-line semicolon import declarations in addition to decoded
TCP-concrete imports. Its two named tests must be in the exact package and directory of the
bound neutral proof and cannot locally redeclare `NeutralRuntime`, `activateFroniusProfile`,
`froniusOverlayProductionPackages`, or `hasTCPConcreteImport`. The disposition-bound workflow
uses the fixed canonical proof source, removes every other direct sibling test source, builds the
exact production package, then runs activation before import-boundary. Its checked-out head/tree
assertion is executable after GitHub expression substitution. For `OVERLAY_REQUIRED`, RED uses the
identical canonical test-source blob named by GREEN, and its sole parent SHA/tree are exactly the
fetched PR base SHA/tree; this prevents a forged test source or an intervening production overlay.
RED proves preparation and build success, activation failure, and no import success; GREEN proves
all four steps succeed.

Conditional outcomes are additional to dependency completion:

| Gate | Gate issue | Progress outcome | Required completed evidence | Blocked scope on NO_GO/STOP |
|---|---|---|---|---|
| CG-M4-LIVE-GO | FMV3-M4-04 | GO | FMV3-M4-05 | All nine M5 issues |
| CG-M5-SEMANTIC-GO | FMV3-M5-03 | GO | FMV3-M5-04 is its completed tested ancestor | FMV3-M5-09 and every later public/private consumer descendant; never FMV3-M5-04 |

Issue completion never substitutes for either GO. `NO_GO` and `STOP` preserve evidence but
are not progress.
The M3 Fronius and M7 Growatt dispositions are not conditional gates: either completed M3
disposition releases M4, and completed Growatt `NO_ADMISSIBLE_PROFILE` releases Huawei
while preserving the pre-published public evidence/unsupported disposition and creating no
code, catalog entry, support claim, or later companion docs change.
FMV3-M1-00 is the one public companion docs issue/PR for both M1 runtime and the complete M2
contract set; it is not replaced or rewritten. PR #89 is predecessor provenance for the
M1-05/M1-06/M2-01 lane. PR #91 is the sole current authority for its corrected fields;
issue #90 is tracking metadata only. There is no
renumbered or replacement M2 docs issue.
The docs lane remains serialized: FMV3-M5-02 is the earlier canonical PV issue, FMV3-M5-04
then implements/tests candidate semantic MCP, FMV3-M5-03 locks that exact version, and
FMV3-M5-09 is the one later PUBLIC_GRAPHQL_M2M_V1 companion issue before FMV3-M5-05.
FMV3-M7-01 follows M5-09 in that docs lane, publishes the complete
Growatt candidate/admission contract before closing, and is companion for M7-02..04. The
same public repository owns M6-00 and M8-00 pre-implementation binding companions plus
M6-03 sanitized post-lab knowledge publication; its repository mutex prevents concurrent
issues/PRs while preserving independent M6 and M8 dependency branches. The
topology/preflight repository mutex permits at most one active issue and one
active PR per repository and serializes preflight through the protected append-only v2 remote
Git-ref CAS claim. The anchored owner actor and HMAC commitment are fixed before first acquisition;
no-bypass integrity ruleset `20195126` forbids deletion/non-fast-forward, while writer ruleset
`20195127` restricts creation/update. The same anchored launcher signs every transition and
fast-forwards a release tombstone only from the exact issue/run/plan-owned acquired SHA, preserving
generation and history. Its exact ledger/generation/SHA/expiry fence is revalidated before each
protected target-repository mutation.

| ID | Milestone | Repo | Depends on | What | Acceptance | Gates | Rollback |
|---|---|---|---|---|---|---|---|
| FMV3-M0-01 | M0 | Project-Helianthus/.github | - | Create the two planned public Modbus repositories under organization governance. | Governance creates empty public helianthus-modbus and helianthus-modbusreg repositories with no Git objects, default branch, bootstrap content, or product code; private governance creation remains deferred to FMV3-M0-04 under future explicit authorization. | governance, licensing, security | Remove only an unused empty repository created in error; once used, correct governance without rewriting history. |
| FMV3-M0-02 | M0 | Project-Helianthus/helianthus-modbus | FMV3-M0-01 | Bootstrap the newly created public Modbus runtime repository. | After M0-01, the sole destination-initialization exception directly pushes one exact no-parent empty-tree root commit to main for issue number 1, with no content or product code; PR number 2 then starts from that exact base and squash-merges the license, Go module, ownership rules, baseline CI, and read-only scope. | governance, licensing, CI | Remove an unused empty bootstrap; once published, deprecate instead of rewriting history. |
| FMV3-M0-03 | M0 | Project-Helianthus/helianthus-modbusreg | FMV3-M0-01 | Bootstrap the newly created public multi-profile registry repository. | After M0-01, the sole destination-initialization exception directly pushes one exact no-parent empty-tree root commit to main for issue number 1, with no content or product code; PR number 2 then starts from that exact base and squash-merges the license, Go module, ownership rules, baseline CI, and no-per-vendor-repo policy. | governance, licensing, CI | Remove an unused empty bootstrap; once published, deprecate instead of rewriting history. |
| FMV3-M0-04 | M0 | Project-Helianthus/.github | FMV3-M0-01 | Under future explicit authorization, create the two empty private binding repositories under organization governance. | Governance creates empty private helianthus-eebus-binding-private and helianthus-matter-binding-private repositories with no Git objects, default branches, bootstrap content, or product code. | governance, licensing, security | Remove only an unused empty repository created in error; once used, correct governance without rewriting history. |
| FMV3-M0-05 | M0 | Project-Helianthus/helianthus-eebus-binding-private | FMV3-M0-04 | Bootstrap the generic private eeBUS output-binding repository after governance creation. | After repository existence is verified, private access, private license, CI, and a public-contract-only dependency policy exist; PV is the first slice, not repository scope. | governance, licensing, security | Remove an unused empty bootstrap or disable access before any artifact is distributed. |
| FMV3-M0-07 | M0 | Project-Helianthus/helianthus-matter-binding-private | FMV3-M0-04 | Bootstrap the generic private Matter output-binding repository after governance creation. | After repository existence is verified, private access, private license, CI, and a public-contract-only dependency policy exist; M8 remains independent of M6. | governance, licensing, security | Remove an unused empty bootstrap or disable access before any artifact is distributed. |
| FMV3-M0-06 | M0 | Project-Helianthus/helianthus-docs-ebus | - | Publish the layer, ownership, licensing, evidence, and public-private direction contracts. | Documentation defines transport through binding boundaries, standard versus vendor profiles, claim labels, clean-room intake, and write deferral. | doc_gate, licensing | Revert the documentation change while no downstream contract is locked; otherwise publish a correction. |
| FMV3-M1-00 | M1 | Project-Helianthus/helianthus-docs-ebus | FMV3-M0-02, FMV3-M0-06 | Publish one bounded public companion contract for the M1 Modbus foundation and complete M2 registry contracts before implementation. | One docs issue/PR defines Modbus framing and the exact phase-1 read-only operations FC03 Read Holding Registers, FC04 Read Input Registers, and FC2B/MEI0E Read Device Identification, including conformity levels, object IDs, segmentation/more-follows, bounds, exceptions, and malformed responses on TCP/RTU. It also defines transport recovery, coalescing identity, RTU qualification, and complete M2 contracts before implementation. | doc_gate, licensing, protocol_interop | Correct the public contract before implementation; after dependent APIs publish, use a versioned forward correction. |
| FMV3-M1-01 | M1 | Project-Helianthus/helianthus-modbus | FMV3-M0-02, FMV3-M1-00 | Implement strict Modbus protocol types, read request/response codecs, exception handling, and bounded limits. | Against merged companion FMV3-M1-00, RED-first tests cover malformed lengths, identity, exceptions, and the exact FC03/FC04/FC2B-MEI0E allowlist. FC03/FC04 ranges preserve distinct provenance. Device Identification tests cover conformity levels, individual/regular/extended object access, object IDs, segmentation/more-follows, bounded aggregation, exceptions, and malformed responses without profile semantics or string normalization. | TDD_RED, CI, doc_gate, transport_gate, security, companion FMV3-M1-00 | Revert the unpublished API or add a compatible correction before downstream release. |
| FMV3-M1-02 | M1 | Project-Helianthus/helianthus-modbus | FMV3-M1-01 | Implement Modbus TCP endpoint ownership, scheduler, pooling, fairness, coalescing, backoff, reconnect, cancellation, and limits. | Deterministic tests prove one endpoint owner and allocator/map per TCP socket plus existing correlation/abandonment behavior. A wire_response_id binds each physical request/range; unequal overlapping compatible reads coalesce, and each dependent logical_view_id/slice replays its exact words/provenance. Cross-unit, cross-table, cross-authorization, cross-generation, and deadline-incompatible reads do not coalesce. Tombstone reuse/exhaustion, bounded correlation, isolated unit/profile state, and absence of profile semantics remain covered. | TDD_RED, CI, doc_gate, transport_gate, operability, companion FMV3-M1-00 | Disable or revert the unpublished TCP runtime entry point and restore or pin the preceding helianthus-modbus revision; do not touch gateway artifacts or persisted gateway state. |
| FMV3-M1-03 | M1 | Project-Helianthus/helianthus-modbus | FMV3-M1-02 | Implement the shared runtime contract for Modbus RTU without coupling it to Fronius phase 1. | RED-first fixture tests cover framing, CRC, silent intervals, abandonment, and timeout/cancellation quarantine. Capability remains disabled by default and experimental with disposition FIXTURE_ONLY_NO_HARDWARE unless RTU_PHYSICAL_QUALIFICATION_V1 records adapter/transceiver identity, baud/topology, measured physical silent intervals, and timeout/cancellation quarantine traces and yields PHYSICALLY_QUALIFIED. No hardware means no enabled/supported claim, but completes fixture conformance and blocks no TCP/Fronius path or TCP-sufficient M1/M7 work. | TDD_RED, CI, doc_gate, transport_gate, hardware_conditional, companion FMV3-M1-00 | Disable RTU registration while retaining the protocol and TCP APIs. |
| FMV3-M1-04 | M1 | Project-Helianthus/helianthus-modbus | FMV3-M1-02, FMV3-M1-03 | Close TCP and RTU conformance, stress, cancellation, and recovery matrices. | TCP and RTU fixture matrices pass with no unexpected fail/xpass, retain every recovery row, and prove bounded FC2B/MEI0E Device Identification framing, segmentation, exception, and malformed-response behavior on both transports. RTU qualification disposition remains explicit, and TCP/Fronius never depends on RTU hardware. | TDD_RED, CI, doc_gate, transport_gate, recovery, companion FMV3-M1-00 | Hold the failing transport disabled and keep passing transports available behind explicit capability flags. |
| FMV3-M1-05 | M1 | Project-Helianthus/helianthus-docs-ebus | FMV3-M1-04 | Publish the OPAQUE_RUNTIME_ACQUISITION_V1 companion contract before corrective runtime code. | Plan authorization for this docs issue does not require its PR to be merged; completion does. The exact bound docs R2 head/tree defines source_kind as runtime or offline_fixture. Only the runtime source, after request-bound correlation proves successful data, the dependent remains attached, its exact logical slice validates, and production is coherent, may issue an opaque non-serializable capability from private source-owned state shared solely by copies of that capability and never by an M2 ledger pointer. Caller booleans and every non-success acquisition receive no capability; offline fixtures remain zero-CAS with no production sample identity. Endpoint recreation and every new acquisition create fresh independent state regardless of visible identity or data and cannot alias, remint, reset, or merge prior state. Capability state moves once from open to claimed, cancelled, failed, or expired; runtime-source-owned CancelOpen uses the exact opaque closed AttemptInstance, after atomic membership close blocks late registration and freezes the ordered member set, and returns only after pre-close registrations, member operations, reclamation, and all open members drain. Claims move unresolved to claim_in_progress before one immutable terminal result; cancellation moves open or sealed attempts through cancelling, drains in-progress claims, and closes only remaining unresolved claims; Seal atomically requires a nonempty exact ordered all-runtime dependency set, exact cardinality, and every claim to be claim_succeeded; empty, fixture-only, mixed, duplicate, omitted, or reordered sets are rejected before capability CAS. Finite-positive byte and field limits validate before allocation, checked retained-state products cannot overflow, and capability, attempt, and claim terminal sequences are pre-reserved nonzero uint64 values that never wrap or reuse. Synchronous sequence-ordered reclamation retains only byte-bounded non-reconstructing tombstones, and normalization records round-trip exactly within admitted bounds. A fresh independent OpenAI review of the exact docs revision must close with all findings resolved or NO_FINDINGS before merge. | doc_gate, licensing, data_integrity, fresh_adversarial_review | Correct the public contract before M1-06; after implementation, publish a versioned forward correction without weakening one-shot or source-origin guarantees. |
| FMV3-M1-06 | M1 | Project-Helianthus/helianthus-modbus | FMV3-M1-04, FMV3-M1-05 | Implement the source-issued opaque single-use runtime acquisition capability. | Authorization requires docs PR #386 merged with the exact bound docs R2 head/tree. Under this issue, an owner-authored harness PR is first and is the sole active PR in helianthus-modbus; it adds only the exact plan-anchored dual-mode workflow, AST guard, docs-lock validator, and exact merged-docs lock, leaves inherited ci.yml and ci_local.sh unchanged, passes required checks and one clean exact-head Codex review, and merges before product work. Only then may the product PR open from exactly the harness merge. Against the merged M1-05 contract, a test-only RED commit with the exact validator-pinned subject precedes implementation; the anchored pull_request workflow must pass its RED diff guard and compile/no-tests step before its exact M1-06 conformance suite fails. Tests prove source-owned post-correlation delivery only after every docs-defined required condition, every excluded non-deliverable outcome, private non-serializable state shared only across copies of the same capability, one-shot CAS, and fresh independent state for endpoint recreation or new acquisition even with matching visible identity or data. New acquisition cannot alias, remint, reset, or merge prior state; every coalesced dependent receives its own capability, copied views racing the same capability produce exactly one winner, and capability state moves once from open to claimed, cancelled, failed, or expired. Runtime-source-owned CancelOpen uses the exact opaque closed AttemptInstance with frozen ordered membership, leaves ledger mutation to M2 linearization, rejects late registration without exposing a capability, isolates stale same-key instances, and returns only after pre-close registrations, member operations, reclamation, and all open members drain. Finite-positive byte and field limits validate before decode, copy, hash, intern, allocation, or retention; terminal sequences are reserved before admission as nonzero uint64 values, never wrap or reuse, and exhaustion rejects new capabilities while preserving existing termination. Terminal state is synchronously removed before return in reserved-sequence order, and only byte-bounded non-reconstructing immutable tombstones remain with lowest-sequence-first eviction. The implementation head passes required checks and the same anchored conformance workflow, then all report-bound exact-parent mutants retain the harness blobs, pass AST and parent-baseline guards, compile, and fail only the mapped mutant test. One official Codex exact-head review and two fresh owner process attestations resolve to NO_FINDINGS before merge. | TDD_RED, CI, doc_gate, data_integrity, concurrency, fresh_adversarial_review, companion FMV3-M1-05 | Disable capability delivery and pin consumers to the last compatible runtime; never replace the token with a serializable or replayable surrogate. |
| FMV3-M2-01 | M2 | Project-Helianthus/helianthus-modbusreg | FMV3-M0-03, FMV3-M1-00, FMV3-M1-01, FMV3-M1-06 | Define profile, codec, catalog, source-observation identity/coherence, and raw-provenance contracts. | Against merged FMV3-M1-00 and M1-05, CI first locks the full merged docs SHA plus policy and manifest SHA-256 values and executes all ledger-owned downstream behavioral tests, including ordered dependency rejection, non-publishable source sets, atomic publish/cancel winners, and closed-projection canary exclusion. Bounded external authorization-evidence selectors identify the M1-06 issue, sequential harness and closing product PRs, merge and RED SHAs, anchored RED/GREEN runs, eight mutation commits/runs, official Codex review, and two owner process attestations. Live GitHub must prove the exact immutable marked/title issue; an earlier owner-authored same-repo harness PR that was the sole active PR, passed required checks and one clean exact-head Codex review before merge, added only the exact plan-anchored dual-mode workflow, AST guard, docs-lock validator, and exact merged-docs lock, preserved inherited ci.yml and ci_local.sh, and is the exact base of the later closing product PR; and the product head/merge tree, one-base-parent squash topology, issue closure, and canonical-main ancestry. The test-only RED ancestor has the exact pinned subject, a bounded non-production first diff page and empty second page, and an exact-SHA exact-PR run of the anchored workflow whose RED guard and compile/no-tests succeed before the exact conformance suite fails. The exact implementation head has required checks pinned to concrete GitHub App IDs plus the same anchored workflow with conformance success. Eight ordered production-Go-only mutant commits each parent directly from GREEN, retain the exact workflow, AST guard, docs-lock validator, and merged-docs lock blobs, match the canonical GitHub patch digest precommitted in the GREEN report, pass executable-AST validation and the mapped parent baseline test, compile, and only then fail the exact mapped mutant test. One official Codex canonical-template zero-inline review and two owner structured process attestations follow GREEN and all mutations and bind the report blob, fixed case digest, and mutation-evidence digest. The owner records attest the fresh OpenAI process but are not represented as independently authenticated OpenAI artifacts; the official Codex review is the authenticated independent product-review surface. The fixed-path closed report binds validator-pinned PASS cases for deliverability exclusions, copy one-winner, stale same-key instance cancellation isolation, terminal outcomes, membership close/registration race, bounds/overflow, sequence exhaustion, and coalesced isolation to exact regular Go test source blobs, exact test declarations, assertion/failure calls, semantic API calls, exact mutation patch digests, and production Go blobs declaring every fixed contract symbol. Runtime acquisitions require the M1 capability while offline_fixture remains untrusted with zero capability CAS and no production sample_id. The independent M2 ledger rejects duplicate exact bounded AttemptKey before state change. Each claim moves unresolved to claim_in_progress only while its attempt is open, records one immutable source result before completion, and cannot retry. Seal atomically requires a nonempty exact ordered all-runtime dependency set, exact cardinality, and every claim to be claim_succeeded; empty, fixture-only, mixed, duplicate, omitted, or reordered sets are rejected before capability CAS. Cancellation linearizes open or sealed to cancelling, blocks later claim admission, seal, and publish, invokes runtime-source-owned CancelOpen by the exact opaque closed AttemptInstance with frozen membership, drains every claim_in_progress, closes only remaining unresolved claims, and then reaches cancelled. Publish() is the one-shot sealed-to-publishing transition over immutable sealed ledger state; publish commit atomically couples the irreversible external effect with publishing-to-published, cancellation wins before that commit with publish_failed and no external effect, while cancellation after commit returns already_published without mutation, and no outcome retries. Finite-positive byte and field limits validate before allocation and cover every retained state, claims per attempt, the checked nonoverflowing product, diagnostics, normalization, and audit tombstones. Attempt and claim terminal sequences are batch-reserved before admission as nonzero uint64 values, never wrap or reuse, and exhaustion rejects new attempts while preserving existing termination. Eligible terminal state is synchronously reclaimed in reserved-sequence order into byte-bounded non-reconstructing immutable tombstones with lowest-sequence-first eviction. Versioned codecs preserve existing wire, logical, sample provenance, and exact normalization-record round-trip within admitted bounds. Hosted GREEN and a fresh independent OpenAI review of the exact RED and implementation revisions are merge blockers. | TDD_RED, CI, doc_gate, data_integrity, concurrency, fresh_adversarial_review, companion FMV3-M1-00, corrective companion FMV3-M1-05 | Version the unpublished contract or add a compatible successor before profile release. |
| FMV3-M2-02 | M2 | Project-Helianthus/helianthus-modbusreg | FMV3-M1-00, FMV3-M2-01 | Implement deterministic profile detection and version-gated read-only probe planning. | Against merged companion FMV3-M1-00, tests prove bounded no-write probes, deterministic ordering, model and firmware gates, ambiguity and no-match failure, and no activation from partial evidence; fixture-only profiles are default-disabled experimental_opt_in, explicit opt-in cannot bypass gates, and auto_eligible requires a matching versioned hardware qualification record with safe mismatch, revocation, disable, and demotion behavior. | TDD_RED, CI, doc_gate, protocol_interop, security, companion FMV3-M1-00 | Disable automatic detection and require an explicit profile selection with the same validation gates. |
| FMV3-M2-03 | M2 | Project-Helianthus/helianthus-modbusreg | FMV3-M1-00, FMV3-M2-01, FMV3-M2-02 | Build sanitized fixture, replay, mutation, and cross-profile conformance harnesses. | Against merged FMV3-M1-00, deterministic fixtures retain codec, detector, qualification, normalization, generation, and torn-read coverage. A required mutation coalesces unequal overlapping reads and proves each linked logical view replays exact words/provenance; cross-unit, cross-table, cross-authorization, cross-generation, and deadline-incompatible mutations must refuse coalescing. | TDD_RED, CI, doc_gate, testability, licensing, companion FMV3-M1-00 | Quarantine a bad fixture or profile version without changing shared runtime behavior. |
| FMV3-M3-01 | M3 | Project-Helianthus/helianthus-docs-ebus | FMV3-M0-06, FMV3-M2-01 | Build a provenance-qualified Fronius and SunSpec evidence packet and sanitized fixture manifest. | One public companion packet defines the evidence, conformance, and disposition record used by FMV3-M3-02 and FMV3-M3-03; sources, model/firmware applicability, standard use, overlay scope, unknowns, and fixture derivation are reviewable without private paths or secrets. | doc_gate, licensing, protocol_interop | Withdraw disputed claims and retain them as labeled hypotheses until revalidated. |
| FMV3-M3-02 | M3 | Project-Helianthus/helianthus-modbusreg | FMV3-M1-02, FMV3-M2-03, FMV3-M3-01 | Implement the minimal SunSpec standard-family slice required by the Fronius phase-1 fixtures. | RED-first fixtures prove standard model-chain parsing, codec signedness/scale interpretation, each applicable declared multi-register word order, intra-word byte order, string packing/padding case, exact raw/sample identity, recorded documentary address normalization, profile-declared coherence, provenance, version gates, transport neutrality, and no vendor assumptions or write path. | TDD_RED, CI, doc_gate, protocol_interop, licensing, companion FMV3-M3-01 | Disable the minimal SunSpec profile version while retaining shared registry contracts. |
| FMV3-M3-03 | M3 | Project-Helianthus/helianthus-modbusreg | FMV3-M3-02 | Determine Fronius phase-1 applicability from Modbus TCP evidence and implement only any evidence-required transport-neutral read-only overlay. | Against companion FMV3-M3-01 and M3-02 conformance, record exactly STANDARD_ONLY or OVERLAY_REQUIRED publicly. Modbus TCP is only the phase-1 evidence and acquisition path; overlay code remains transport-neutral and cannot import TCP types or gate activation on TCP. Every Fronius production token is confined to the exact profiles/fronius namespace. CI runs the fixed import-boundary test through an exact executable fail-closed runtime source scanner which enumerates the current proof-package directory, inspects every direct non-test .go source including build-constrained and implicitly excluded filenames, parses imports with go/parser and go/token, unquotes with strconv.Unquote, sorts deterministically, and returns directory/read/parse/unquote errors to t.Fatal. Its exact TCP predicate scans every import-path component with the validator's net/TCP/Modbus-TCP rule and returns the offending path. Direct production and named-test imports are restricted to the validator-pinned transport-neutral standard-library allowlist, with only the scanner's go/parser, go/token, os, path/filepath, runtime, and testing additions admitted for named tests, while module-local, third-party, and every other import fail closed. CI also runs the fixed activation test through a zero-field non-TCP fake with a compile-time assertion against the exact NeutralRuntime interface; its sentinel error must be returned by a real runtime call. Under STANDARD_ONLY, the bound test-only minimal neutral adapter has no imports or additional declarations and calls the one zero-argument error-returning NeutralRuntime method directly. Under OVERLAY_REQUIRED, the exact production set is profiles/fronius/activation.go plus profiles/fronius/overlay.go, where activateFroniusProfile in the former delegates to activateFroniusReadOnlyOverlay in the latter, whose only behavior is the same neutral runtime call; neither file has imports or extra declarations. The fixed activation test must traverse both production functions, so unused stubs, extra production sources, and TCP-gated branches or literals fail closed. Both test declarations and scanner helpers must be in one fixed named source, registry/fronius_overlay_test.go for STANDARD_ONLY or profiles/fronius/fronius_overlay_test.go for OVERLAY_REQUIRED. The exact workflow removes every other direct *_test.go sibling in that proof package before a standalone go build of its exact package target for STANDARD_ONLY or every package under ./profiles/fronius/... for OVERLAY_REQUIRED, then runs activation first and import-boundary second against the exact proof package; each exact YAML job/step has no skip, redirect, custom shell, override, or continue-on-error. Candidate overlay production sources have no build constraints, init function, or test-only fake/sentinel references. The validator binds the interface, activation, scanner, and predicate declarations and requires assertion control flow to consume scan errors, offending paths, or the sentinel result. Both dispositions retain evidence, fixtures, live scope, identity/provenance/coherence, read-only behavior, and green conformance CI. STANDARD_ONLY proves the complete exact PR-base-to-head tree diff contains only tests, fixtures, docs, the bound workflow, or fixed evidence and therefore produces no implementation commit, overlay namespace source, or overlay RED evidence. OVERLAY_REQUIRED requires the exact-tree profiles/fronius production pair and no other production source, plus a RED ancestor whose complete exact parent-tree-to-RED-tree diff is nonempty and test-only, and a failed run on the same PR/workflow ID/path/blob. RED preparation and build succeed, activation specifically fails, and import has no success result; GREEN requires successful preparation, build, activation, and import evidence. Either disposition unblocks M4. | CI, doc_gate, protocol_interop, licensing, TDD_RED_IF_OVERLAY_REQUIRED, companion FMV3-M3-01 | Invalidate the disposition when evidence changes; disable an admitted overlay while minimal SunSpec and raw Modbus access remain available. |
| FMV3-M4-01 | M4 | Project-Helianthus/helianthus-ebusgateway | FMV3-M1-02, FMV3-M3-03 | Implement gateway package internal/modbusadapter and compose one owned TCP endpoint behind the existing protocol-agnostic adapter interface. | internal/modbusadapter is the only gateway package importing helianthus-modbus or helianthus-modbusreg and has real-module integration tests; gateway core tests use the interface and a fake adapter to prove endpoint exclusivity, unit fairness, compatible coalescing, source validity/time and unchanged sample/poll/dependency/wire-response/logical-view identity, mixed or incoherent rejection, disable, and cancellation without gateway-owned canonical freshness. | TDD_RED, CI, transport_gate, operability | Disable the Modbus endpoint configuration and restart without changing eBUS behavior. |
| FMV3-M4-02 | M4 | Project-Helianthus/helianthus-ebusgateway | FMV3-M4-01 | Expose bounded raw Modbus and profile-observation MCP tools before semantic promotion. | MCP tests expose sanitized raw reads, detector and activation-lifecycle evidence, profile identity, source validity/timestamps, physical request/range wire_response_id, each dependent logical_view_id/slice, sample/poll/dependency identity, normalization provenance, limits, and exact-view replay while rejecting writes and unbounded ranges. | TDD_RED, CI, security, MCP_first | Disable the MCP tools while retaining internal read-only polling and diagnostics. |
| FMV3-M4-03 | M4 | Project-Helianthus/helianthus-ha-addon | FMV3-M4-01 | Add validated endpoint configuration, secret handling, disable switch, health, and restart recovery for the add-on. | Tests prove atomic config validation, no secret logging, bounded restart behavior, explicit disable, and fallback to the previous image or binary. | TDD_RED, CI, security, recovery | Restore the previous add-on image and disable Modbus configuration. |
| FMV3-M4-04 | M4 | Project-Helianthus/helianthus-ebusgateway | FMV3-M4-02, FMV3-M4-03 | Run the required Fronius real-device TCP read-only smoke and recovery gate. | A reproducible lab record proves opt-in detection before qualification, bounded polling, raw MCP parity, coherent sample/wire-response/logical-view identity, source observation stop/resume across disconnect/reconnect, generation integrity, no writes, and no regression to existing gateway traffic; it records exactly GO, NO_GO, or STOP, only GO permits downstream progress, and the matching GO record qualifies only the tested profile/hardware tuple. | CI, transport_gate, hardware_required, recovery | Disable the endpoint and restore the prior gateway/add-on pair if smoke or recovery fails. |
| FMV3-M4-05 | M4 | Project-Helianthus/helianthus-docs-ebus | FMV3-M3-01, FMV3-M4-04 | Publish sanitized Fronius live applicability evidence and retained unknowns. | Documentation packages the exact FMV3-M4-04 GO, NO_GO, or STOP outcome, tested model, firmware and profile versions, gateway mode, smoke result, unknown fields, and provenance without device secrets or private material paths; packaging a non-GO outcome preserves evidence but unlocks no M5 issue. | doc_gate, licensing, hardware_required | Retract unsupported applicability and return affected fields to Unknown. |
| FMV3-M5-01 | M5 | Project-Helianthus/helianthus-ebusreg | FMV3-M5-02 | Define protocol-independent canonical PV identities, quantities, quality, freshness, counters, and compatibility rules. | Work starts only after FMV3-M5-02 has merged the public contract from FMV3-M4-04 GO and packaged FMV3-M4-05 evidence; RED-first tests define stable IDs, units, canonical quality, freshness deadlines and transitions, stale/unavailable behavior, counter rollover/reset, provenance references, schema versioning, and compatibility from source validity/timestamps without delegating canonical policy upstream. | TDD_RED, CI, doc_gate, data_integrity | Before publication remove the candidate; after publication retain schema/IDs and use a compatible successor or make disabled data unavailable. |
| FMV3-M5-02 | M5 | Project-Helianthus/helianthus-docs-ebus | FMV3-M4-05 | Publish and merge the public canonical PV contract documentation from M4 evidence before semantic implementation. | Work starts only after FMV3-M4-04 records GO and FMV3-M4-05 packages that evidence; before FMV3-M5-01 implementation and semantic lock, documentation merges with proposed version, meanings, source-validity-to-canonical-quality boundary, freshness deadlines, compatibility, evidence requirements, and private-consumer boundary. | doc_gate, licensing | Before publication revise/remove candidate docs; after publication correct or deprecate them with the retained normative schema. |
| FMV3-M5-03 | M5 | Project-Helianthus/helianthus-execution-plans | FMV3-M5-04 | Lock the exact golden- and live-tested candidate semantic MCP version. | Fresh adversarial review examines the exact FMV3-M5-04 mapping, semantic MCP version, golden results, live Fronius trace, evidence, quality/freshness behavior, and compatibility, then records GO, NO_GO, or STOP. Completion is not success; GO permits FMV3-M5-09 and later consumers, while NO_GO/STOP preserves raw and candidate semantic MCP for remediation but unlocks no consumer. | semantic_lock, adversarial_review, operator_go | Revoke an unpublished GO decision; after publication, issue a new compatible contract and lock decision. |
| FMV3-M5-04 | M5 | Project-Helianthus/helianthus-ebusgateway | FMV3-M5-01, FMV3-M5-02 | Implement the candidate Fronius-to-canonical mapping and semantic MCP before lock. | Golden and live Fronius tests run the exact candidate semantic MCP version, propagate sample_id, poll_generation_id and complete dependency/wire-response/logical-view identity unchanged, reject mixed/incoherent samples, preserve source validity/timestamps, apply only ebusreg-owned canonical transitions, and prove raw-to-canonical traceability. This issue is outside CG-M5-SEMANTIC-GO and creates no GraphQL or consumer contract. | TDD_RED, CI, MCP_first, hardware_required | Before publication remove promotion; after publication retain schema/IDs, disable mapping, report data unavailable, and keep raw MCP. |
| FMV3-M5-09 | M5 | Project-Helianthus/helianthus-docs-ebus | FMV3-M5-03 | Publish the one public PUBLIC_GRAPHQL_M2M_V1 companion contract after semantic MCP lock and before GraphQL implementation. | After FMV3-M5-04 semantic MCP and explicit FMV3-M5-03 GO on that exact tested version, one serialized public docs issue/PR later than FMV3-M5-02 publishes the locked schema projection, external access/security/channel, compatibility/versioning, credential lifecycle, and recovery for PUBLIC_GRAPHQL_M2M_V1; it introduces no raw registers or subscription and merges before FMV3-M5-05. | doc_gate, licensing, semantic_lock, security | Revise before GraphQL publication; after publication retain the documented version and issue a compatible correction or deprecation. |
| FMV3-M5-05 | M5 | Project-Helianthus/helianthus-ebusgateway | FMV3-M5-09 | Implement GraphQL parity for the locked PV projection and publish the documented public machine-to-machine access contract. | Against merged companion FMV3-M5-09, GraphQL schema, resolver, authorization, and snapshots match semantic MCP and preserve canonical quality, time, unit, freshness/unavailable state, source identity, and compatible schema IDs; one externally routable endpoint contract is reachable from a separately deployed service-client context and proves noninteractive authentication/authorization, least privilege, bounded polling/rate/resources, and credential provisioning/rotation/revocation/disable/recovery. Credential-bearing external use requires the documented authenticated confidential channel with verified server identity before credentials are sent; plaintext external access and untrusted identity fail closed without prescribing the channel/auth mechanism, weakening security, adding a subscription, or exposing raw registers through GraphQL. | TDD_RED, CI, API_parity, doc_gate, security, companion FMV3-M5-09 | Before publication remove candidate fields; after publication retain schema/IDs and forward-fix or return unavailable while the capability is disabled. |
| FMV3-M5-06 | M5 | Project-Helianthus/helianthus-ebusgateway | FMV3-M5-05 | Add the Portal semantic PV view and authenticated bounded raw diagnostics/register exploration in the existing serialized gateway issue. | Portal semantic tests consume GraphQL only and preserve canonical identity, units, quality, freshness/unavailable state, and source timestamps; a separate authenticated/authorized raw explorer reuses the bounded raw MCP service contract with endpoint/function/range budgets, read-only allowlists, rate/resource limits, secret redaction and audit evidence, without adding raw registers to semantic GraphQL or importing Modbus outside the local adapter. | TDD_RED, CI, consumer_parity, security | Disable semantic and raw Portal routes independently while published GraphQL schema/IDs remain available or explicitly unavailable and raw MCP remains policy-controlled. |
| FMV3-M5-07 | M5 | Project-Helianthus/helianthus-ha-integration | FMV3-M5-06 | Consume the locked GraphQL PV contract in Home Assistant. | RED-first tests cover discovery, stable unique IDs, units, unavailable and stale states, restart, migration, and absence of raw Modbus coupling. | TDD_RED, CI, consumer_parity | Disable entities while retaining published schema/IDs and expose unavailable; restore only a compatible integration release. |
| FMV3-M5-08 | M5 | Project-Helianthus/helianthus-ha-addon | FMV3-M5-06, FMV3-M5-07 | Package and prove a recoverable public Fronius-to-HA and service-client rollout. | Exact-version smoke proves raw MCP, semantic MCP, GraphQL, GraphQL-only semantic Portal, authenticated bounded raw Portal exploration, and HA availability; from a separately deployed external service context it repeats endpoint reachability, noninteractive authn/authz, least privilege, bounded polling/rate/resources, credential provisioning/rotation/revocation/disable/recovery, and authenticated confidential-channel server-identity verification for exactly PUBLIC_GRAPHQL_M2M_V1. Tests reject plaintext external access and untrusted server identity before credential use, without prescribing the mechanism, plus prove restart, independent surface disable, pre-publication rollback, post-publication compatibility recovery, and no schema-less replacement for a published schema. | CI, hardware_required, recovery, release_gate, security | Before publication restore the prior pair; after publication use only a schema-compatible image or keep schema/IDs and report disabled data unavailable. |
| FMV3-M6-00 | M6 | Project-Helianthus/helianthus-docs-ebus | FMV3-M5-08 | Publish the sanitized public eeBUS binding and SHIP/SPINE companion contract. | Before private implementation, one permissively publishable companion defines PUBLIC_GRAPHQL_M2M_V1 ingress, SHIP/SPINE discovery, TLS/pairing, trust lifecycle, reconnect/revocation/reset, capability negotiation, identity, encoding, PV exchange, security boundaries, and the rule that reusable protocol knowledge cannot remain private. | doc_gate, licensing, protocol_interop, security | Correct before implementation or publish a compatible successor; no private protocol code may bypass it. |
| FMV3-M6-01 | M6 | Project-Helianthus/helianthus-eebus-binding-private | FMV3-M0-05, FMV3-M6-00 | Deploy a generic private eeBUS output binding that polls exactly the packaged public GraphQL machine-to-machine PV contract as its only semantic ingress. | A test-only RED commit is CI-observed before implementation; deployment/config tests consume exactly PUBLIC_GRAPHQL_M2M_V1 as externally tested by FMV3-M5-08, use its noninteractive least-privilege authorization, bounded polling/resources, and authenticated confidential channel with verified server identity, reject plaintext external access and untrusted server identity before credential use without prescribing the mechanism, reject incompatible schema/contract versions, and prove credential disable/recovery, reconnect/backoff, explicit capability disable, and unavailable/stale handling from ingress through output; deterministic peer tests also cover minimum SHIP/SPINE discovery, SHIP TLS/pairing, trust persistence, reconnect, revocation/reset, disable recovery, session/capability negotiation, identity, encoding, and a complete PV exchange, with no invented GraphQL subscription, Modbus, Fronius, register, gateway-internal logic, or public eeBUS repo. | TDD_RED, CI, licensing, private_boundary, protocol_interop, security, recovery, companion FMV3-M6-00 | Disable the PV capability or revert the private artifact without changing public contracts. |
| FMV3-M6-02 | M6 | Project-Helianthus/helianthus-eebus-binding-private | FMV3-M6-01 | Test actual myVaillant interoperability as a lab hypothesis. | Record exactly GO, NO_GO, or STOP; completion is not progress and GO is the sole objective success. GO requires an enabled and qualified live Fronius endpoint throughout the run and at least one traced observation that is available, non-stale, and generated after the recorded lab-run start. The same observation identity and value must traverse PUBLIC_GRAPHQL_M2M_V1 and eeBUS to an accepted myVaillant-side observable with matching canonical/source identity, value, unit/value semantics, quality, source-observation time, and receipt time. Replayed, synthetic, retained-cache-only, fixture-only, or simulator-only input and handshake/packet observation alone cannot GO. Existing identity/time/quality fields suffice; no new public schema field is required. NO_GO/STOP retains sanitized honest evidence, completes no plan objective, unlocks no success, and adds no vendor logic or public-schema distortion. | CI, hardware_required, protocol_interop, security, recovery | Disable the private export and preserve the lab trace as sanitized evidence. |
| FMV3-M6-03 | M6 | Project-Helianthus/helianthus-docs-ebus | FMV3-M6-02 | Publish sanitized reusable eeBUS and myVaillant interoperability findings. | Publish every reusable SHIP/SPINE, trust, identity, encoding, capability, and myVaillant interoperability finding with provenance/licensing and secret removal. If required knowledge cannot be published permissively, record STOP and make no support or interoperability claim; private-only protocol knowledge cannot satisfy M6. | doc_gate, licensing, protocol_interop, security | Withdraw disputed public claims and retain STOP without changing private/public runtime contracts. |
| FMV3-M7-01 | M7 | Project-Helianthus/helianthus-docs-ebus | FMV3-M0-06, FMV3-M1-04, FMV3-M2-03, FMV3-M5-09 | Build separate SunSpec, Growatt, and Huawei evidence and applicability packets. | After FMV3-M5-09, the companion publishes existing SunSpec/Growatt contracts and, for every vendor candidate, enumerates each detector operation/PDU and proves it is in the versioned helianthus-modbus phase-1 runtime allowlist. Every SmartLogger/S-Dongle candidate also receives a complete public admission packet or NO_ADMISSIBLE_PROFILE. EMMA discriminators are inventoried or unavailable; semantics remain deferred and missing discrimination blocks Huawei automatic eligibility. | doc_gate, licensing, protocol_interop | Quarantine disputed facts and keep affected support states experimental or Unknown. |
| FMV3-M7-02 | M7 | Project-Helianthus/helianthus-modbusreg | FMV3-M1-04, FMV3-M2-03, FMV3-M3-03, FMV3-M7-01 | Expand the minimal M3 SunSpec slice with additional versioned standard models without vendor assumptions. | Conformance proves model-chain parsing, standard units and scales, every applicable declared word order, intra-word byte order, string packing/padding case, transport neutrality, version gates, unknown model handling, and codec-version provenance. | TDD_RED, CI, doc_gate, protocol_interop, hardware_conditional, companion FMV3-M7-01 | Disable the affected standard profile version in the catalog. |
| FMV3-M7-03 | M7 | Project-Helianthus/helianthus-modbusreg | FMV3-M7-02 | Decide Growatt profile admission and add profiles or overlays only when evidence permits. | Against the already-merged companion FMV3-M7-01, record exactly PROFILE_ADMITTED or NO_ADMISSIBLE_PROFILE. PROFILE_ADMITTED alone triggers RED-first fixtures/code for bounded read-only conformance and requires no later companion docs change. NO_ADMISSIBLE_PROFILE performs no implementation or later companion docs change, preserves the pre-published public evidence and explicit unsupported disposition, creates no catalog/support claim, and releases FMV3-M7-04 without a GO gate. | CI, licensing, protocol_interop, hardware_conditional, doc_gate, TDD_RED_IF_PROFILE_ADMITTED, companion FMV3-M7-01 | Disable an admitted Growatt profile without changing shared contracts, or revise NO_ADMISSIBLE_PROFILE only when new qualified evidence reopens admission. |
| FMV3-M7-04 | M7 | Project-Helianthus/helianthus-modbusreg | FMV3-M7-03 | Add clean-room Huawei SmartLogger and S-Dongle profiles with branch and gateway gates. | For each M7-01 SmartLogger/S-Dongle candidate, record PROFILE_ADMITTED or NO_ADMISSIBLE_PROFILE. Admission requires every detector probe to map to the versioned runtime operation allowlist; modbusreg cannot frame a protocol PDU. Only admission triggers RED-first code and positive fixtures. Non-admission creates no code/catalog/support claim. EMMA and insufficiently distinguished negatives remain fail-closed. | CI, doc_gate, protocol_interop, licensing, hardware_conditional, TDD_RED_IF_PROFILE_ADMITTED, companion FMV3-M7-01 | Disable the affected Huawei profile and return unconfirmed values to Unknown. |
| FMV3-M7-05 | M7 | Project-Helianthus/helianthus-modbusreg | FMV3-M7-04 | Close cross-profile detection, ambiguity, version, mutation, activation, and lifecycle conformance. | A mixed catalog selects at most one eligible primary and retains activation lifecycle tests. EMMA and insufficiently distinguished negative fixtures return only no_match or insufficient_evidence, never activate SmartLogger/S-Dongle, and keep Huawei automatic eligibility blocked whenever reliable gateway/model/software/version discrimination is unavailable. | TDD_RED, CI, protocol_interop, testability | Pin the last passing catalog and disable new profile versions independently. |
| FMV3-M8-00 | M8 | Project-Helianthus/helianthus-docs-ebus | FMV3-M5-08 | Publish the sanitized public Matter binding companion contract. | Before private implementation, one permissively publishable companion defines the PUBLIC_GRAPHQL_M2M_V1 ingress boundary, Matter identity/capability/encoding projection, trust and credential lifecycle, compatibility, stale/unavailable behavior, recovery, and forbidden private/internal imports without disclosing licensed binding implementation. | doc_gate, licensing, protocol_interop, security | Correct before implementation or publish a compatible successor; no private protocol code may bypass it. |
| FMV3-M8-01 | M8 | Project-Helianthus/helianthus-matter-binding-private | FMV3-M0-07, FMV3-M8-00 | Implement a generic private Matter output-binding kernel from exactly PUBLIC_GRAPHQL_M2M_V1. | After packaged FMV3-M5-08 ancestry, RED-first security tests consume exactly PUBLIC_GRAPHQL_M2M_V1 by authenticated bounded query/polling, reject incompatible contract versions, require noninteractive least privilege, a confidential channel and verified server identity, reject plaintext/untrusted identity, and prove credential provision/rotation/revocation/disable/recovery plus reconnect, disable, and stale/unavailable handling. Imports from helianthus-modbus, helianthus-modbusreg, gateway internals, undocumented network paths, and GraphQL subscriptions are rejected. | TDD_RED, CI, licensing, private_boundary, security, recovery, companion FMV3-M8-00 | Disable or revert the private artifact without changing public contracts. |
| FMV3-M8-02 | M8 | Project-Helianthus/helianthus-matter-binding-private | FMV3-M8-01 | Add and test PV as the first Matter binding capability slice. | Conformance tests preserve identity, units, quality, stale and unavailable behavior; no Matter claim blocks M6 or changes the locked PV contract. | TDD_RED, CI, consumer_parity, hardware_optional | Disable the PV capability while retaining the generic binding kernel. |
